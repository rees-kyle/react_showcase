const Components = () => {
    return (
        <div>
            <h1 className="underline">Components and Props</h1>

            <h2>Components in React</h2>
            <p>
                Components are the building blocks of any React application.
                They allow you to split the UI into independent, reusable pieces, making the code easier to manage and reason about.
            </p>
        </div>
    );
};

export default Components;