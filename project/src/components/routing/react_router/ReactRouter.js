const ReactRouter = () => {
    return (
        <div>
            <h1 className="underline">React Router</h1>
            <p>
                To set up routing in a React project, you'll use React Router,
                a popular library for managing navigation between different views.
                Here's a breakdown of how to implement the different routing functionalities:
            </p>
        </div>
    );
};

export default ReactRouter;