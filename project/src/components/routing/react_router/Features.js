const Features = () => {
  return (
    <div>
      <br/>
      <hr/>
      <h2>Additional Features</h2>
      <h3>Navigation</h3>
      <p>For internal navigation, use the 'Link' component, which works like an anchor tag.</p>
      <h3>Redirects</h3>
      <p>You can use the 'Navigate' component to redirect users to a different route.</p>
      <h3>Protected Routes</h3>
      <p>Protected routes can be created by wrapping them in a custom component to restrict access to certain users.</p>
    </div>
  );
};

export default Features;
